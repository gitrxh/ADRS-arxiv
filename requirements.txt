gymnasium==0.29.1
# legacy gym: webshop/search/sokoban env_package 直接 `import gym`(gym.make /
# gym.Env + 0.26 的 disable_env_checker),不是 gymnasium。缺它 nebula 上会
# ModuleNotFoundError: No module named 'gym'。pin 0.26.2(与 numpy<2 兼容)。
gym==0.26.2
stable-baselines3==2.6.0
alfworld
hydra-core>=1.3.0
omegaconf>=2.3.0
tqdm
numpy
datasets
sentencepiece
protobuf

# --- Webshop env deps (only needed for *_webshop experiments) ---
# NOTE: pyserini also needs a JDK 11 + the spacy en_core_web_sm model + a built
# Lucene index. train.sh's webshop setup block handles those at job startup.
beautifulsoup4==4.15.0
cleantext==1.1.4
Flask==2.1.2
Werkzeug==2.1.0
rank_bm25==0.2.2
thefuzz==0.19.0
selenium==4.2.0
pyserini==0.17.0
faiss-cpu
